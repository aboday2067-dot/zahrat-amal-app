# 📚 توثيق الأنظمة الجديدة - زهرة الأمل

## ✅ الأنظمة المكتملة

### 1️⃣ نظام المعرف الفريد (Unique ID System)

**الموقع**: `lib/services/unique_id_service.dart`

**الوصف**: نظام توليد معرفات فريدة لكل مستخدم بصيغة موحدة.

**التنسيق**: `ZA-YYYY-NNNNNN`
- `ZA`: اختصار زهرة الأمل
- `YYYY`: السنة (مثال: 2025)
- `NNNNNN`: رقم تسلسلي (6 أرقام)

**مثال**: `ZA-2025-001234`

**الميزات**:
- ✅ توليد معرفات فريدة تلقائياً
- ✅ التحقق من صحة المعرف
- ✅ استخراج السنة والرقم التسلسلي
- ✅ دعم تسجيل الدخول بالمعرف الفريد

**الاستخدام**:
```dart
// توليد معرف فريد جديد
final uniqueId = await UniqueIdService.generateUniqueId();
// نتيجة: ZA-2025-001234

// التحقق من صحة المعرف
bool isValid = UniqueIdService.isValidUniqueId('ZA-2025-001234');

// استخراج السنة
int? year = UniqueIdService.getYearFromUniqueId('ZA-2025-001234');
```

---

### 2️⃣ نظام الإشعارات المتقدم (Advanced Notification System)

**الموقع**: `lib/services/notification_service.dart`

**الوصف**: نظام إرسال إشعارات عبر قنوات متعددة.

**القنوات المدعومة**:
- 📧 البريد الإلكتروني (Email)
- 📱 الرسائل النصية (SMS)

**أنواع الإشعارات**:
1. **إشعار تسجيل الدخول**: يُرسل عند كل تسجيل دخول جديد
2. **إشعار الطلبات**: تأكيد الطلب ومتابعة حالته
3. **إشعار الترحيب**: للمستخدمين الجدد

**الميزات**:
- ✅ إرسال متزامن عبر البريد والهاتف
- ✅ تسجيل محلي للإشعارات
- ✅ دعم القوالب المخصصة
- ✅ وضع التطوير للاختبار

**الاستخدام**:
```dart
// إرسال إشعار بريد إلكتروني
await NotificationService.sendEmailNotification(
  email: 'user@example.com',
  subject: 'عنوان الرسالة',
  message: 'محتوى الرسالة',
);

// إرسال إشعار SMS
await NotificationService.sendSmsNotification(
  phone: '+249912345678',
  message: 'نص الرسالة',
);

// إرسال إشعار تسجيل دخول
await NotificationService.sendLoginNotification(
  email: 'user@example.com',
  phone: '+249912345678',
  userName: 'أحمد محمد',
  uniqueId: 'ZA-2025-001234',
);
```

---

### 3️⃣ نظام تسجيل الدخول المحسّن (Enhanced Login System)

**الموقع**: `lib/screens/auth/enhanced_login_screen.dart`

**الوصف**: شاشة تسجيل دخول متقدمة تدعم طرق متعددة.

**طرق تسجيل الدخول**:
1. 📧 **البريد الإلكتروني**: user@example.com
2. 📱 **رقم الهاتف**: +249912345678
3. 🆔 **المعرف الفريد**: ZA-2025-001234

**الميزات**:
- ✅ كشف تلقائي لنوع المعرف
- ✅ واجهة مستخدم ديناميكية
- ✅ دعم كامل للعربية (RTL)
- ✅ إرسال إشعار تلقائي عند تسجيل الدخول

**الحسابات التجريبية**:
- **المسؤول**: `admin@zahrat.sd` / `admin123`
- **مستخدم عام**: أي بريد أو هاتف / `123456`
- **معرف فريد**: `ZA-2025-001234` / `123456`

---

### 4️⃣ نظام الإيصالات (Receipt System)

**الموقع**: `lib/services/receipt_service.dart`

**الوصف**: نظام شامل لتوليد وإرسال الإيصالات.

**الميزات الرئيسية**:
- ✅ توليد إيصالات تلقائياً
- ✅ تنسيقات متعددة (نص، HTML)
- ✅ حساب الضريبة (15%)
- ✅ رسوم التوصيل
- ✅ إرسال عبر البريد والSMS

**مكونات الإيصال**:
```
📄 معلومات الإيصال:
- رقم الإيصال
- رقم الطلب
- التاريخ والوقت
- الحالة (مدفوع/قيد الانتظار/ملغى)

👤 معلومات العميل:
- الاسم
- البريد الإلكتروني
- رقم الهاتف

🏪 معلومات التاجر:
- اسم المتجر

🛍️ المنتجات:
- اسم المنتج
- الكمية
- سعر الوحدة
- الإجمالي

💰 الإجماليات:
- المجموع الفرعي
- الضريبة (15%)
- رسوم التوصيل
- الإجمالي النهائي

💳 طريقة الدفع
```

**الاستخدام**:
```dart
// إنشاء إيصال
final receipt = ReceiptService.generateReceipt(
  orderId: 'ORD123',
  buyerId: 'user_id',
  buyerName: 'أحمد محمد',
  buyerEmail: 'ahmed@example.com',
  buyerPhone: '+249912345678',
  merchantId: 'merchant_id',
  merchantName: 'متجر السودان',
  items: [
    ReceiptItem(
      name: 'منتج 1',
      quantity: 2,
      unitPrice: 100.0,
      total: 200.0,
    ),
  ],
  paymentMethod: 'cash',
);

// تحويل إلى نص
String textReceipt = ReceiptService.formatReceiptAsText(receipt);

// تحويل إلى HTML
String htmlReceipt = ReceiptService.formatReceiptAsHtml(receipt);

// إرسال عبر البريد
await ReceiptService.sendReceiptByEmail(receipt);

// إرسال عبر SMS
await ReceiptService.sendReceiptBySMS(receipt);

// إرسال عبر جميع القنوات
final results = await ReceiptService.sendReceiptMultiChannel(receipt);
```

---

### 5️⃣ نظام الدردشة (Chat System) - قيد التطوير

**الموقع**: 
- `lib/models/message_model.dart` (النماذج)
- `lib/services/chat_service.dart` (الخدمات)
- `lib/screens/chat/` (الشاشات)

**الحالة**: تم إنشاء البنية الأساسية، لكن تم تأجيل التكامل النهائي لمرحلة لاحقة.

**الميزات المخططة**:
- 💬 محادثات بين المشتري والتاجر
- 📦 محادثات بين المشتري ومكتب التوصيل
- 🚚 محادثات بين التاجر ومكتب التوصيل
- 📎 دعم المرفقات (صور، ملفات)
- ✓ علامات القراءة
- 🔔 إشعارات الرسائل الجديدة

**أنواع المحادثات**:
```dart
enum ChatType {
  buyerMerchant,    // مشتري ↔ تاجر
  buyerDelivery,    // مشتري ↔ توصيل
  merchantDelivery, // تاجر ↔ توصيل
}
```

**أنواع الرسائل**:
```dart
enum MessageType {
  text,      // نص
  image,     // صورة
  file,      // ملف
  order,     // معلومات طلب
  location,  // موقع
}
```

---

## 📊 إحصائيات المشروع

### الملفات والأكواد
- **إجمالي ملفات Dart**: 39 ملف
- **الشاشات**: 24 شاشة
- **الخدمات**: 6 خدمات
- **النماذج**: 3 نماذج

### الخدمات المتوفرة
```
lib/services/
├── auth_service.dart              # خدمة المصادقة
├── unique_id_service.dart         # خدمة المعرفات الفريدة ✨ جديد
├── notification_service.dart      # خدمة الإشعارات ✨ محدّث
├── receipt_service.dart           # خدمة الإيصالات ✨ جديد
├── chat_service.dart              # خدمة الدردشة ⏳ قيد التطوير
└── mock_data_service.dart         # خدمة البيانات الوهمية
```

---

## 🎯 الخطوات التالية (المرحلة القادمة)

### الأولوية العالية
1. **إكمال نظام الدردشة**
   - إصلاح مشاكل Directionality
   - دمج الشاشات مع التطبيق
   - اختبار المحادثات المباشرة

2. **تكامل الإيصالات مع الطلبات**
   - ربط نظام الإيصالات بصفحة الدفع
   - إرسال تلقائي عند إتمام الطلب
   - حفظ الإيصالات في قاعدة البيانات

3. **تفعيل APIs حقيقية**
   - استبدال المحاكاة بخدمات حقيقية
   - تكامل مع SendGrid للبريد
   - تكامل مع Twilio للSMS

### الأولوية المتوسطة
4. **تحسينات UX**
   - رسوم متحركة للإشعارات
   - feedback بصري أفضل
   - مؤشرات تحميل محسّنة

5. **التحليلات والتقارير**
   - تقارير الإيصالات
   - إحصائيات الرسائل
   - تحليل نشاط المستخدمين

---

## 🔒 الأمان والخصوصية

### نظام المعرف الفريد
- ✅ معرفات فريدة لا يمكن تكرارها
- ✅ تخزين آمن في SharedPreferences
- ✅ التحقق من الصحة قبل الاستخدام

### نظام الإشعارات
- ✅ تسجيل محلي للإشعارات
- ✅ حماية البيانات الشخصية
- ⚠️ **للإنتاج**: استخدم HTTPS و API Keys آمنة

### نظام الإيصالات
- ✅ معلومات مشفرة
- ✅ أرقام إيصالات فريدة
- ✅ طوابع زمنية دقيقة

---

## 📞 الدعم الفني

للمساعدة أو الاستفسارات حول الأنظمة الجديدة:
- 📧 Email: support@zahrat.sd
- 💬 Chat: سيتم تفعيله قريباً
- 📱 Phone: قريباً

---

## 📝 ملاحظات التطوير

### نصائح للمطورين
1. **المعرف الفريد**: استخدمه كمفتاح أساسي للمستخدمين
2. **الإشعارات**: اختبر في وضع التطوير أولاً
3. **الإيصالات**: تحقق من الصيغة قبل الإرسال
4. **الدردشة**: انتظر اكتمال التطوير قبل الاستخدام

### التبعيات المطلوبة
```yaml
dependencies:
  shared_preferences: ^2.5.3  # للتخزين المحلي
  intl: ^0.19.0               # للتنسيقات
  http: ^1.5.0                # للاتصال بالAPIs
  provider: ^6.1.5            # لإدارة الحالة
```

---

**آخر تحديث**: ديسمبر 17، 2025  
**الإصدار**: 2.0.0  
**الحالة**: ✅ جاهز للاختبار

**تم التطوير بـ ❤️ في السودان 🇸🇩**
