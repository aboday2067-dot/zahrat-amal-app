name: zahrat_amal
description: "سوق السودان الذكي - زهرة الأمل"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase Core (LOCKED versions for stability)
  firebase_core: 3.6.0
  cloud_firestore: 5.4.3
  
  # UI Dependencies
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter:
  uses-material-design: true
