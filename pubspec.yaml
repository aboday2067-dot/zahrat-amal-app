name: zahrat_amal
description: "سوق السودان الذكي - زهرة الأمل"
publish_to: 'none'
version: 2.5.0+5

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (LOCKED versions for stability)
  firebase_core: 3.6.0
  cloud_firestore: 5.4.3
  firebase_messaging: 15.1.3
  firebase_storage: 12.3.2
  
  # UI & State Management
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.4
  provider: 6.1.5+1
  
  # Payments & Location
  url_launcher: ^6.3.1
  geolocator: ^13.0.2
  google_maps_flutter: ^2.10.0
  
  # Utilities
  intl: ^0.19.0
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  http: 1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter:
  uses-material-design: true
