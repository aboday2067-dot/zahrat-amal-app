name: zahrat_amal_admin
description: "لوحة إدارة زهرة الأمل - Admin Dashboard"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (LOCKED versions - same as main app)
  firebase_core: 3.6.0
  cloud_firestore: 5.4.3
  firebase_storage: 12.3.2
  firebase_messaging: 15.1.3
  
  # UI & State Management
  cupertino_icons: ^1.0.8
  provider: 6.1.5+1
  
  # Utilities
  intl: ^0.19.0
  image_picker: ^1.1.2
  http: 1.5.0
  shared_preferences: ^2.5.4
  
  # Charts & Analytics
  fl_chart: ^0.69.0
  
  # Security
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter:
  uses-material-design: true
